var config = {};

config.env = {
    name: 'developer',
};

config.database = {
    name: 'vcard3',
    collection: '',
};

config.front = {
    uri: '/',
};

config.backend = {
};

module.exports = config;